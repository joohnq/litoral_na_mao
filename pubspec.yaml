name: litoral_na_mao
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0-35.0.dev <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  go_router:
  responsive_builder:
  unicons:
  ionicons:
  carousel_slider:
  flutter_map:
  latlong2:
  cupertino_icons:
  url_launcher:
  http:
  dio:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
